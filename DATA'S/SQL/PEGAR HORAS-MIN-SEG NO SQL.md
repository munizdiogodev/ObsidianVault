
```SQL
    RIGHT('0' + CAST(DATEPART(HOUR, DT_CHAMADA) AS VARCHAR), 2) + ':' +

    RIGHT('0' + CAST(DATEPART(MINUTE, DT_CHAMADA) AS VARCHAR), 2) + ':' +

    RIGHT('0' + CAST(DATEPART(SECOND, DT_CHAMADA) AS VARCHAR), 2) AS HORA_STRING
```